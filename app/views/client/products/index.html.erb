<% @products.each do |product| %>
 <h2><a href="/client/products/<%= product["id"] %>"><%= product["name"] %></a></h2>
  <h3>Price(USD): <%= product["price"] %></h3>
  <h3><%= product["image_url"] %></h3>
  <h4>Description: <%= product["description"] %></h4>
  
<% end %>